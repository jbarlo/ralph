{
  "name": "ralph",
  "version": "0.1.0",
  "description": "Agent loop cli",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "prepare": "tsc"
  },
  "dependencies": {
    "jimkit-cli": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
